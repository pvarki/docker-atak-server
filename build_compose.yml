services:
  v560:
    image: ${HUB_DOCKER_REPO:-docker.io/}pvarki/tak-server-dist:5.6-RELEASE-57
    build:
      platforms:
        - linux/amd64
        - linux/arm64
      context: .
      dockerfile: Dockerfile_build
      target: takgov_files
      args:
        TAK_VERSION: "5.6"
        TAK_RELEASE: "57"
